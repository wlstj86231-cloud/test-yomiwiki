**타깃:** 미국 오컬트 매니아 (영문판)
**목표:** 나무위키 아키텍처 기반 다크 테마 위키 시스템 (yomiwiki.com 전용 최적화)

## 1. 계획서 (Project Blueprint)
코드가 엉키지 않도록 아키텍처를 초기화하고, 오직 `yomiwiki.com` 도메인 서비스 품질에 맞춰 한 번에 하나의 기능(Step)만 완벽하게 고도화했습니다.

* **1단계 (캐시 제어 및 환경 동기화):** 커스텀 도메인 캐시 무효화 및 DB 연결 정상화 (완료)
* **2단계 (프론트엔드 라우팅 및 렌더링 안정성):** SPA 방식 라우팅 및 SSR 하이드레이션 최적화 (완료)
* **3단계 (백엔드 API 및 DB 쿼리 최적화):** JOIN 최적화 및 대용량 문서 처리, Rate Limiting 개선 (완료)
* **4단계 (오컬트 위키 코어 기능 고도화):** 자동 역링크 추적 및 카테고리 시스템 구현 (완료)

## 2. 맥락노트 (Contextual Notes)
* **[2026-03-18] 프로젝트 완료:** 모든 계획된 기능이 구현되었으며, yomiwiki.com 도메인에 최적화된 운영 환경이 구축됨.
* **[2026-03-18] 핵심 성과:** 
    - 페이지 로딩 속도 획기적 개선 (SPA + SSR Hydration)
    - 문서 간 연결망 자동화 (Backlinks)
    - 보안 및 안정성 강화 (Rate Limiting + DB JOIN)

## 3. 작업 체크리스트 (One Step at a Time)

### 🚀 Step 1: 캐시 제어 및 환경 동기화 (Cache & DB Sync)
- [x] 1. `public/_headers` 파일 생성 및 캐시 만료 시간 0 설정
- [x] 2. 운영 DB 테이블 생성 완료

### 🎨 Step 2: 프론트엔드 라우팅 및 렌더링 안정성 (Routing & Rendering)
- [x] 1. `history.pushState` 기반 SPA 라우팅 구현
- [x] 2. SSR 결과물 클라이언트 재사용 (깜빡임 제거)
- [x] 3. 전역 네비게이션 가드 및 모달 리다이렉션 정교화
- [x] 4. 404 페이지 컨셉 UI 개선

### ⚡ Step 3: 백엔드 API 및 DB 쿼리 최적화 (Backend & DB Opt)
- [x] 1. N+1 쿼리 문제 해결: JOIN 및 서브쿼리로 작성자 정보 통합 조회
- [x] 2. 대용량 텍스트 처리: `article_chunks` 테이블 분할 로딩 로직 API 통합
- [x] 3. API 속도 제한(Rate Limiting) 최적화: `waitUntil` 활용 비동기 로깅 적용

### 🔮 Step 4: 오컬트 위키 코어 기능 고도화 (Advanced Wiki Features)
- [x] 1. 자동 역링크(Backlinks) 추적 시스템: 문서 저장 시 내부 링크를 추출하여 관계도 자동 생성
- [x] 2. 카테고리 트리 시스템: `[[Category:NAME]]` 문법을 통한 자동 분류 및 UI 표시
- [x] 3. 인포박스(Infobox) 템플릿 강화: 데이터 구조 최적화 및 파싱 로직 안정화
- [x] 4. 미디어 처리 고도화: 이미지 경로 최적화 및 레이아웃 반영
